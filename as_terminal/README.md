### Follow the following steps to run this terminal program

1. Navigate to termial for this directory
2. run command `python app.py`

**After running the command the program should start in the terminal.**
